NEXT_PUBLIC_SUPABASE_URL=https://qlxejaxzpotozbixccvu.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=https://api.astropal.app

# Server only. Bypasses row level security, so it must NEVER be prefixed
# NEXT_PUBLIC_ - that would ship it in the browser bundle. Only
# app/api/analytics uses it, and only after checking the caller's JWT and
# app_role. See ANALYTICS.md.
SUPABASE_SERVICE_ROLE_KEY=
